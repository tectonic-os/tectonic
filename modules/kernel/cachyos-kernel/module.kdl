description "CachyOS kernel and companions, with Secure Boot module signing"

supports "fedora"

provides "kernel-devel"
provides-file "/usr/share/secureboot/sb_cert.der"
provides-file "/usr/lib/kernel-build/kernel-package"

provides-file "/usr/libexec/kernel-devel-helpers.sh" build-only=#true

requires "initramfs-generation"
collects "dracut.modules" into="/usr/lib/kernel-build/dracut.modules" priority=500

arg "KERNEL"
secret "mok_privkey"
