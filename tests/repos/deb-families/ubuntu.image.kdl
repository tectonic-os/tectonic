image {
    name "Ubuntu"

    base "docker.io/library/ubuntu:26.04" {
        family "ubuntu"
    }

    modules {
        module "debian-family"
        module "core/menu"
        module "core/shared"
    }
}
