image {
    name "Ubuntu"

    base "ghcr.io/bootcrew/ubuntu-bootc:latest" {
        family "ubuntu"
        provides "initramfs-generation"
        signed #true
    }

    modules {
        module "core/shared"
    }
}
