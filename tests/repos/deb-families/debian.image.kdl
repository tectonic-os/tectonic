image {
    name "Debian"

    base "docker.io/library/debian:forky" {
        family "debian"
    }

    modules {
        module "debian-family"
        module "core/menu"
        module "core/shared"
    }
}
