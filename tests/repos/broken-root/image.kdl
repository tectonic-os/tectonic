image {
    name "Same"

    base "quay.io/fedora/fedora-bootc:44" {
        family "fedora"
        provides "rechunking"
    }

    modules {
        module "fedora-family"
        module "core/hello"
    }
}

image {
    name "Same"

    base "quay.io/fedora/fedora-bootc:44" {
        family "fedora"
        provides "rechunking"
    }

    modules {
        module "fedora-family"
        module "core/hello"
    }
}
