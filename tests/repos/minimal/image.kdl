image {
    name "Minimal"

    base "quay.io/fedora/fedora-bootc:44" {
        family "fedora"
        provides "rechunking"
    }

    layout {
        bootloader "grub2"
    }

    modules {
        module "fedora-family"
        module "core/hello"
    }
}
