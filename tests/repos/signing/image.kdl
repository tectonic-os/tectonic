image {
    name "Signing"

    base "quay.io/fedora/fedora-bootc:44" {
        family "fedora"
        provides "rechunking" "initramfs-generation"
    }

    modules {
        module "kernel/signed-kernel"
        module "drivers/kvmfr"
    }
}
