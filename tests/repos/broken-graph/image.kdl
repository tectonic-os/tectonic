image {
    name "Broken Graph"

    base "quay.io/fedora/fedora-bootc:44" {
        family "fedora"
        provides "rechunking" "boot policy"
        requires "bootc-base"
    }

    modules {
        module "core/one"
        module "core/two"
        module "core/three"
    }
}
