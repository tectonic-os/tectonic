description "Requires a capability nothing in this image provides"

supports "fedora"

packages {
    fedora "fastfetch"
}

helpers "lib/one/shared.sh"
requires "package-manager"
requires "apparmor-policy"
requires-file "/usr/bin/nothing"
after "logging"
