  x `collects` needs into=, where the build puts them
   ,-[./modules/core/menu/module.kdl:5:1]
 4 | 
 5 | collects "justfile.inc" priority=500
   : ^^^^|^^^
   :     `-- incomplete
   `----
  help: `collects "justfile.inc" into="/usr/share/just/justfile.apps"
        priority=500`

  x `priority` is a number from 0 to 9999
   ,-[./modules/core/absent/module.kdl:5:28]
 4 | 
 5 | contributes "justfile.inc" priority=90000
   :                            ^^^^^^^|^^^^^^
   :                                   `-- not a priority
 6 | contributes "justfile.inc" priority=100
   `----
  help: it becomes the NNNN in the staged filename, so four digits is the
        whole range there is

  x `core/absent` orders a justfile.inc it does not ship
   ,-[./modules/core/absent/module.kdl:5:1]
 4 | 
 5 | contributes "justfile.inc" priority=90000
   : ^^^^^|^^^^^
   :      `-- nothing to order
 6 | contributes "justfile.inc" priority=100
   `----
  help: shipping the file is what contributes it; this node only says where it
        lands

  x `core/absent` orders a justfile.inc it does not ship
   ,-[./modules/core/absent/module.kdl:6:1]
 5 | contributes "justfile.inc" priority=90000
 6 | contributes "justfile.inc" priority=100
   : ^^^^^|^^^^^
   :      `-- nothing to order
   `----
  help: shipping the file is what contributes it; this node only says where it
        lands

