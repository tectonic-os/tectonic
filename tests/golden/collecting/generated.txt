==== generated/lib/dkms-helpers.sh
==== generated/lib/fetch-helpers.sh
==== generated/lib/kernel-helpers.sh
==== generated/lib/repo-helpers.sh
==== generated/lib/selinux-helpers.sh
==== generated/lib/sign-helpers.sh
==== generated/lib/wrap-helpers.sh
==== generated/collecting.d/core/menu.sh
#!/usr/bin/env bash
# GENERATED FILE, do not edit.
set -euxo pipefail

MODDIR=/ctx/modules/core/menu
export MODDIR
==== generated/collecting.d/core/power.sh
#!/usr/bin/env bash
# GENERATED FILE, do not edit.
set -euxo pipefail

MODDIR=/ctx/modules/core/power
export MODDIR

mkdir -p /usr/share/menu/justfile.apps.d
cat /ctx/modules/core/power/justfile.inc > /usr/share/menu/justfile.apps.d/0900-core-power.part
==== generated/collecting.d/core/network.sh
#!/usr/bin/env bash
# GENERATED FILE, do not edit.
set -euxo pipefail

MODDIR=/ctx/modules/core/network
export MODDIR

mkdir -p /usr/share/menu/justfile.apps.d
cat /ctx/modules/core/network/justfile.inc > /usr/share/menu/justfile.apps.d/0100-core-network.part
==== generated/collecting.d/finalize.sh
#!/usr/bin/env bash
# GENERATED FILE, do not edit.
set -euxo pipefail

# ---- /usr/share/menu/justfile.apps ----
parts=()
for part in /usr/share/menu/justfile.apps.d/0100-core-network.part /usr/share/menu/justfile.apps.d/0900-core-power.part; do
    [ -f "$part" ] || continue
    parts+=("$part")
done
if [ ${#parts[@]} -gt 0 ]; then
    cat "${parts[@]}" > /usr/share/menu/justfile.apps
fi
rm -rf /usr/share/menu/justfile.apps.d

# ---- /opt relocation ----
mkdir -p /usr/lib/opt
tmpfiles=/usr/lib/tmpfiles.d/zz-opt-symlinks.conf
printf 'd /var/opt 0755 root root -\n' > "$tmpfiles"
for d in /opt/*/; do
    [ -d "$d" ] || continue
    name="$(basename "$d")"
    cp -a "$d" "/usr/lib/opt/${name}"
    esc="${name// /\\x20}"
    printf 'L+ /var/opt/%s - - - - /usr/lib/opt/%s\n' "$esc" "$esc" >> "$tmpfiles"
done
rm -rf /opt
mv /opt.bak /opt

# ---- module presets ----
apply_module_presets() {
    local scope="$1" dir="$2" flag=() f verb unit
    [ "$scope" = user ] && flag=(--global)
    for f in "$dir"/45-module-*.preset; do
        [ -f "$f" ] || continue
        while read -r verb unit; do
            case "$verb" in
                enable) systemctl "${flag[@]}" enable "$unit" ;;
                disable) systemctl "${flag[@]}" disable "$unit" ;;
                *) ;;
            esac
        done < "$f"
    done
}
apply_module_presets system /usr/lib/systemd/system-preset
apply_module_presets user /usr/lib/systemd/user-preset
==== generated/collecting.graph.md
# Collecting capability graph

GENERATED FILE, do not edit.

An arrow points from a provider to what needs it, dotted for `after`,
which orders the build without requiring anything. Layers build left to
right.

```mermaid
graph LR
    base["quay.io/fedora/fedora-bootc:44"]
    m0["core/menu"]
    m1["core/power"]
    m2["core/network"]
```

## Capabilities

| Name | Kind | Provided by | Required by | After |
|---|---|---|---|---|
| `rechunking` | capability | `base` |  |  |
==== generated/collecting.graph.json
{
  "image": "collecting",
  "capabilities": [
    {
      "name": "rechunking",
      "kind": "capability",
      "build_only": false,
      "provider": "base",
      "required_by": [],
      "after": []
    }
  ],
  "overrides": [],
  "suppressed": []
}
==== generated/plan.json
