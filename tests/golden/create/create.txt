==== modules/my-editor/module.kdl
description "My Editor"

supports "fedora"
provides "editor"

packages "nano"
==== modules/plain/module.kdl
description "plain"

supports "fedora"
==== example.image.kdl
image {
    name "Example"
    url "https://github.com/someone/example"
    issues-url "https://github.com/someone/example/issues"

    base "quay.io/fedora/fedora-bootc:44" {
        family "fedora"
        provides "rechunking" "initramfs-generation" "selinux-policy"
        provides-file "/usr/sbin/sshd" "/usr/bin/bootc" "/usr/bin/bootupctl" "/usr/bin/podman" "/usr/bin/crun"
        signed #false
    }

    modules {
        module "my-editor"
        source "one" {
            module "fedora-family"
        }
    }
}
==== server.image.kdl
image {
    name "Server"
    url "https://github.com/someone/example"
    issues-url "https://github.com/someone/example/issues"

    base "quay.io/fedora/fedora-bootc:44" {
        family "fedora"
        provides "rechunking" "initramfs-generation" "selinux-policy"
        provides-file "/usr/sbin/sshd" "/usr/bin/bootc" "/usr/bin/bootupctl" "/usr/bin/podman" "/usr/bin/crun"
        signed #false
    }

    modules {
        module "my-editor"
        source "one" {
            module "fedora-family"
        }
    }
}
==== check
