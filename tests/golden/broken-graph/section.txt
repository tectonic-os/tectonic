### Base Image
FROM quay.io/fedora/fedora-bootc:44

## Build phases and modules

# ---- core/one ----
RUN --mount=type=bind,from=ctx,source=/modules/core/one,target=/ctx/modules/core/one \
    --mount=type=bind,from=ctx,source=/lib,target=/ctx/lib \
    --mount=type=cache,target=/var/cache \
    --mount=type=cache,target=/var/log \
    --mount=type=tmpfs,target=/tmp \
    bash /ctx/lib/run-module.sh /ctx/modules/core/one

# ---- core/two ----
RUN --mount=type=bind,from=ctx,source=/modules/core/two,target=/ctx/modules/core/two \
    --mount=type=bind,from=ctx,source=/lib,target=/ctx/lib \
    --mount=type=cache,target=/var/cache \
    --mount=type=cache,target=/var/log \
    --mount=type=tmpfs,target=/tmp \
    bash /ctx/lib/run-module.sh /ctx/modules/core/two

# ---- core/three ----
RUN --mount=type=bind,from=ctx,source=/modules/core/three,target=/ctx/modules/core/three \
    --mount=type=bind,from=ctx,source=/lib,target=/ctx/lib \
    --mount=type=cache,target=/var/cache \
    --mount=type=cache,target=/var/log \
    --mount=type=tmpfs,target=/tmp \
    bash /ctx/lib/run-module.sh /ctx/modules/core/three

# ---- flavour gate ----
ARG FLAVOUR

# ---- finalize hook order ----
ARG FINALIZE_ORDER=""

# ---- collected file destinations ----
ARG COLLECT_TARGETS=""

# ---- image version ----
ARG IMAGE_VERSION=dev

# ---- image identity ----
ARG IMAGE_ID="broken-graph"
ARG IMAGE_NAME="Broken Graph"
ARG IMAGE_REGISTRY=""

