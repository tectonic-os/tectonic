==== the registry
one
two
==== the catalog
one/browser  A browser, from the first collection
two/browser  A browser, from the second collection (requires flatpak)
one/custom-kernel  A kernel the image pins, swapped in over the base's own
one/fedora-family  dnf5 and its plugins, which every fedora build layer reaches for
one/flatpak  Flatpak client and a daily update timer
one/signature-policy  Sigstore signature verification for the image's own updates
==== copy flatpak
copied modules/flatpak
==== copy browser
ambiguous: one, two
==== copy one/browser
copied modules/browser
==== copy two/browser
`browser` was copied from `one`; refusing the namesake from `two`
==== copy nosuch
no module called `nosuch` in one, two
==== copy one/nosuch
no module called `nosuch` in one
==== copy flatpak
`flatpak` was already copied from `one`
==== the tree
modules/.gitkeep
modules/browser/module.kdl
modules/browser/provenance.kdl
modules/flatpak/files/usr/share/example/remotes.list
modules/flatpak/module.kdl
modules/flatpak/provenance.kdl
==== the record
imported "one" {
    content "b1d3c80ac19b0db6acf256c6e50b59e60aca7be95e6ada7a0d048ea2cb4385a7"
}
==== check
==== modified
[]
==== modified after an edit
["modules/flatpak"]
