==== generated/lib/dkms-helpers.sh
==== generated/lib/fetch-helpers.sh
==== generated/lib/kernel-helpers.sh
==== generated/lib/repo-helpers.sh
==== generated/lib/selinux-helpers.sh
==== generated/lib/sign-helpers.sh
==== generated/lib/wrap-helpers.sh
==== scripts/lint.sh
==== scripts/render-iso-config.sh
==== scripts/tect.sh
==== generated/suppressed.d/apps/browser.sh
#!/usr/bin/env bash
# GENERATED FILE, do not edit.
set -euxo pipefail

MODDIR=/ctx/modules/apps/browser
export MODDIR
==== generated/suppressed.d/finalize.sh
#!/usr/bin/env bash
# GENERATED FILE, do not edit.
set -euxo pipefail

# ---- /opt relocation ----
mkdir -p /usr/lib/opt
tmpfiles=/usr/lib/tmpfiles.d/zz-opt-symlinks.conf
printf 'd /var/opt 0755 root root -\n' > "$tmpfiles"
for d in /opt/*/; do
    [ -d "$d" ] || continue
    name="$(basename "$d")"
    cp -a "$d" "/usr/lib/opt/${name}"
    esc="${name// /\\x20}"
    printf 'L+ /var/opt/%s - - - - /usr/lib/opt/%s\n' "$esc" "$esc" >> "$tmpfiles"
done
rm -rf /opt
mv /opt.bak /opt

# ---- module presets ----
apply_module_presets() {
    local scope="$1" dir="$2" flag=() f verb unit
    [ "$scope" = user ] && flag=(--global)
    for f in "$dir"/45-module-*.preset; do
        [ -f "$f" ] || continue
        while read -r verb unit; do
            case "$verb" in
                enable) systemctl "${flag[@]}" enable "$unit" ;;
                disable) systemctl "${flag[@]}" disable "$unit" ;;
                *) ;;
            esac
        done < "$f"
    done
}
apply_module_presets system /usr/lib/systemd/system-preset
apply_module_presets user /usr/lib/systemd/user-preset
==== generated/suppressed.graph.md
# Suppressed capability graph

GENERATED FILE, do not edit.

An arrow points from a provider to what needs it, dotted for `after`,
which orders the build without requiring anything. Layers build left to
right.

```mermaid
graph LR
    base["ghcr.io/ublue-os/bazzite:stable"]
    m0["apps/browser"]
    base -->|"/usr/bin/flatpak, flatpak"| m0
```

## Capabilities

| Name | Kind | Provided by | Required by | After |
|---|---|---|---|---|
| `/usr/bin/flatpak` | file | `base` | `apps/browser` |  |
| `flatpak` | capability | `base` | `apps/browser` |  |
| `rechunking` | capability | `base` |  |  |

## Suppressed

The base already provides everything these declare, so nothing builds
them.

| Module | Provides |
|---|---|
| `apps/flatpak` | `flatpak`, `/usr/bin/flatpak` |
==== generated/suppressed.graph.json
{
  "image": "suppressed",
  "capabilities": [
    {
      "name": "/usr/bin/flatpak",
      "kind": "file",
      "build_only": false,
      "provider": "base",
      "required_by": [
        "apps/browser"
      ],
      "after": []
    },
    {
      "name": "flatpak",
      "kind": "capability",
      "build_only": false,
      "provider": "base",
      "required_by": [
        "apps/browser"
      ],
      "after": []
    },
    {
      "name": "rechunking",
      "kind": "capability",
      "build_only": false,
      "provider": "base",
      "required_by": [],
      "after": []
    }
  ],
  "overrides": [],
  "suppressed": [
    {
      "module": "apps/flatpak",
      "provides": [
        "flatpak",
        "/usr/bin/flatpak"
      ]
    }
  ]
}
==== generated/plan.json
