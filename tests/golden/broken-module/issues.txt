  x `core/nameless` is listed twice
    ,-[./image.kdl:10:9]
  9 |     modules {
 10 |         module "core/nameless"
    :         ^^^|^^
    :            `-- first here
 11 |         module "core/missing"
 12 |         module "core/nameless"
    :         ^^^|^^
    :            `-- and again here
 13 |     }
    `----
  help: a module builds once per flavour it is listed under

  x unknown node `flies`
   ,-[./modules/core/unlisted/module.kdl:4:1]
 3 | 
 4 | flies "a kite"
   : ^^|^^
   :   `-- not part of the schema
 5 | provides "Shell|Tools"
   `----
  help: docs/schema.md documents every node a manifest may hold

  x invalid capability name `Shell|Tools`
   ,-[./modules/core/unlisted/module.kdl:5:1]
 4 | flies "a kite"
 5 | provides "Shell|Tools"
   : ^^^^|^^^
   :     `-- lowercase, digits and dashes, starting with a letter
   `----
  help: the name is written into the generated graph as a mermaid label and a
        markdown table cell, neither of which quotes anything

  x unknown node `drives`
   ,-[./modules/core/nameless/module.kdl:3:1]
 2 | 
 3 | drives "a truck"
   : ^^^|^^
   :    `-- not part of the schema
 4 | satisfies {
   `----
  help: docs/schema.md documents every node a manifest may hold

  x `cis-fedora` has no rules listed
   ,-[./modules/core/nameless/module.kdl:5:5]
 4 | satisfies {
 5 |     cis-fedora
   :     ^^^^^|^^^^
   :          `-- nothing to cover
 6 |     "" "1.1.1.1"
   `----

  x benchmark `stig` is declared twice
   ,-[./modules/core/nameless/module.kdl:8:5]
 7 |     stig "R1"
 8 |     stig "R2"
   :     ^^|^
   :       `-- already declared above
 9 | }
   `----

  x a benchmark name is required inside `satisfies`
   ,-[./modules/core/nameless/module.kdl:6:5]
 5 |     cis-fedora
 6 |     "" "1.1.1.1"
   :     ^|
   :      `-- empty name
 7 |     stig "R1"
   `----
  help: `satisfies { cis-fedora "1.1.1.1" }`

  x `core/nameless` declares no description
  help: one line, present tense, no trailing period; it names the module in
        the resolved build summary

  x `core/nameless` declares no `supports`
  help: a module has to say which base families it can build on, so a
        portability gap surfaces at lint rather than mid-build

  x `core/missing` has no module.kdl
    ,-[./image.kdl:11:9]
 10 |         module "core/nameless"
 11 |         module "core/missing"
    :         ^^^|^^
    :            `-- every module needs a manifest
 12 |         module "core/nameless"
    `----
  help: create ./modules/core/missing/module.kdl; modules/_template/module-
        name/module.kdl is a copy-me reference

  x `core/nameless` does not support the `fedora` base family
  help: add `supports "fedora"` to the manifest

